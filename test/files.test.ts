// import {readState, writeState} from "../src/files"

describe("files", () => {
    it("can be saved and restored", async () => {
        const chainHeight = 20123123n
        // const filepath = `/tmp/${Math.random()}`
        // await writeState(filepath, chainHeight)
        // const result = await readState(filepath)
        // expect(result).not.toBeNull()
        // expect(result?.chainHeight).toEqual(chainHeight)
    })
})